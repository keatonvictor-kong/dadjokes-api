_format_version: "1.1"
services:
  - name: dadjokes
    protocol: http
    host: localhost
    port: 80
    path: /%7BUPSTREAM_SERVER_URL%7D
    plugins: []
    routes:
      - tags:
          &a1
          - OAS3_import
          - ABC
        name: dadjokes-getDadJokes
        methods:
          - GET
        paths:
          - /random
        strip_path: false
    tags: *a1
